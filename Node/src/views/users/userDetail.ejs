<!DOCTYPE html>
<html lang="es">
    <%- include('../partials/head', {title: 'userDetail', estilo:'/stylesheets/form-style.css'}) %>
    <body>
        <%- include('../partials/header') %>
        <main>          
            <form name="Detail" action="/users/detail/<%= user.id %>?_method=PUT" method="POST">
                <h2>Perfil de <%= user.name %> </h2></br>

                <div class="foto">
                    <label for="imagenUsuario">
                        <img 
                            class="img" 
                            src="/images/users/profileImages/<%= user.avatar%>" 
                            alt="imagenUsuario"
                            style="border-radius: 10%; border:solid 1px var(--button);box-shadow: 2px 2px 6px;"
                        >
                    </label>
                </div>

                <div class="campo">
                    <label for="id">
                        <i class="fa-solid fa-user"></i>
                         ID:
                    </label>
                    <span style="text-align: left;">
                        <%= user.id%>
                    </span>
                </div>

                <div class="campo">
                    <label for="nombre">
                        <i class="fa-solid fa-user"></i>
                         Nombre:
                    </label>
                    <span style="text-align: left;">
                        <%= user.name%>
                    </span>
                </div>

                <div class="campo">
                    <label for="apellido">
                        <i class="fa-solid fa-user"></i>
                         Apellido:
                    </label>
                    <span style="text-align: left;">
                        <%= user.lastName %>
                    </span>
                </div>
                
                <div class="campo">
                    <label for="email">
                        <i class="fa-solid fa-at"></i>
                         Email:
                    </label>
                    <span style="text-align: left;">
                        <%= user.email %> 
                    </span> 
                </div>
                
                <div class="campo">
                    <label for="fecha_de_nacimiento">
                        <i class="fa-solid fa-calendar-days"></i>
                         Fecha de Nacimiento:
                    </label>
                    <span style="text-align: left;">
                        <%= user.birthdate %>
                    </span>
                </div>
                <div class="campo">
                    <label> Activar Usuario: </label>
                    <label>
                        Si
                        <input 
                        type="radio" 
                        name="activarUser"
                        id="activarUser"
                        value= "true"
                        <% if (user.isActive) { %>
                            checked
                        <% } %>
                        >
                    </label>
                        
                    <label>
                        No
                        <input 
                        type="radio" 
                        name="activarUser"
                        id="desactivarUser"
                        value= "false"
                        <% if (!user.isActive) { %>
                            checked
                        <% } %>
                        >
                    </label>
                </div>
                <div class="campo">
                    <label> Hacer Admin: </label>
                    <label>
                        Si
                        <input 
                        type="radio" 
                        name="userAdmin"
                        id="hacerAdmin"
                        value= "true"
                        <% if (user.isAdmin) { %>
                            checked
                        <% } %>
                        >
                    </label>
                        
                    <label>
                        No
                        <input 
                        type="radio" 
                        name="userAdmin"
                        id="deshacerAdmin"
                        value= "false"
                        <% if (!user.isAdmin) { %>
                            checked
                        <% } %>
                        >
                    </label>
                </div>
                
                <div class="botones">
                    <button class="text-left" type="submit">Editar</button>
                </div>
                <div class="forgot">
                    <a class="text-left" href="/users/list">Volver</a>
                    <a class="text-right" href="/users/delete/<%= user.id %>">Eliminar Usuario</a>
                </div>
            </form>
        </main>
        <%- include('../partials/footer') %>
        <script src="/js/users/userDetail.js"></script>
    </body>
</html>